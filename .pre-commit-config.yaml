# To use:
#
#     pre-commit run -a
#
# Or:
#
#     pre-commit install  # (runs every time you commit in git)
#
# To update this file:
#
#     pre-commit autoupdate
#
# See https://github.com/pre-commit/pre-commit

repos:
  # Standard hooks
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v5.0.0
    hooks:
      # LFS is not enabled for this repo, so killing this for now.
      # - id: check-added-large-files
      - id: check-ast
      - id: check-case-conflict
      - id: check-docstring-first
      - id: check-merge-conflict
      - id: check-symlinks
      - id: check-xml
      - id: check-yaml
      - id: debug-statements
      - id: end-of-file-fixer
        exclude: (\.(svg|stl|dae|obj|mtl|xml))$
      - id: mixed-line-ending
      - id: trailing-whitespace
      - id: fix-byte-order-marker

  # markdownlint hook
  - repo: https://github.com/igorshubovych/markdownlint-cli
    rev: v0.49.1
    hooks:
      - id: markdownlint
        # Disable max line length check for markdown
        args: ["--disable", "~MD013"]

  # shellcheck hook
  - repo: https://github.com/shellcheck-py/shellcheck-py
    rev: v0.11.0.1-1
    hooks:
      - id: shellcheck

  # Python hooks
  - repo: https://github.com/asottile/pyupgrade
    rev: v3.21.2
    hooks:
    -   id: pyupgrade
        args: [--py36-plus]

  - repo: https://github.com/psf/black
    rev: 26.5.1
    hooks:
      - id: black
        args: ["--line-length=120"]

  - repo: https://github.com/pycqa/flake8
    rev: 7.3.0
    hooks:
    - id: flake8
      args:
        - --max-line-length=120
        - '--per-file-ignores=phoebe_mujoco_config/scripts/wheel_code_gen.py: E501'

  # CPP hooks
  - repo: https://github.com/pre-commit/mirrors-clang-format
    rev: v19.1.1
    hooks:
      - id: clang-format
        files: \.(c|cc|cxx|cpp|frag|glsl|h|hpp|hxx|ih|ispc|ipp|java|m|proto|vert)$

  # TODO: Add ament checks for ROS packages, if required

  # Spellcheck in comments and docs
  - repo: https://github.com/codespell-project/codespell
    rev: v2.4.3
    hooks:
      - id: codespell
        exclude: \.(svg|stl|dae)
        args: ["--ignore-words-list", "hande, Hart"]

# Exclude generated assests
exclude: |
  (?x)^(
    phoebe_mujoco_config/description/.*
  )$
